runtime: python37
entrypoint: gunicorn -b :$PORT wishlist.wsgi

handlers:
- url: /static
  static_dir: static/

- url: /.*
  script: auto

env_variables:
  TRAVELER_PW: Porknuggets123

beta_settings:
  cloud_sql_instance: wishlist-django-310504:us-central1:wishlist-db